output: dist/
releases:
  - name: dev
    jobs:
      # - name: linux-appimage
      #   package:
      #     platform: linux
      #     target: appimage
      - name: linux-deb
        package:
          platform: linux
          target: deb
      - name: linux-zip
        package:
          platform: linux
          target: zip
      - name: macos-dmg
        package:
          platform: macos
          target: dmg
      - name: macos-zip
        package:
          platform: macos
          target: zip
      - name: windows-exe
        package:
          platform: windows
          target: exe
      - name: windows-msix
        package:
          platform: windows
          target: msix
      - name: windows-zip
        package:
          platform: windows
          target: zip
