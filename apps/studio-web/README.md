# studio-web

TanStack Start application for the Fastforge monorepo.

Run commands from the repository root:

```bash
pnpm studio:dev
pnpm studio:lint
pnpm studio:typecheck
pnpm studio:build
```

Reusable UI components live in `packages/studio-ui` and are imported through the
`studio-ui` workspace package.
